# events/api/urls.py (API URLs)
 
from django.urls import path
from . import views
 

app_name = 'events-api'

urlpatterns = [
    # path('events/', api_views.EventListCreate.as_view(), name='event-list'),
    # # ... API routes
]